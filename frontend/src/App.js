// The KAYE / TEN TOES experience is a pure HTML/CSS/vanilla-JS build
// served from /public (index.html, style.css, script.js).
// React is intentionally inert here so it never interferes with the site.
export default function App() {
  return null;
}
